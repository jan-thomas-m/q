name: test
run-name: A simple noop test
on:
  push:
  pull_request:
    # Sequence of patterns matched against refs/heads
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-slim
    # runs-on: ubuntu-latest
    steps:
      # - uses: actions/checkout@v6
      - run: echo Hello world!
      # - uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      # - run: npm install -g bats
      # - run: bats -v
